# Coaches

Coach paths use:

```text
coaches/<language>/<goal-or-exam>/
```

Each coach owns one practice contract and one Receipt schema. Shared routing
lives only in the root `AGENTS.md`.

| Path | Status |
|---|---|
| `english/general-speaking` | active |
| `english/toefl-ibt-2026-speaking` | active |
| `english/ielts-speaking` | reviewed-not-session-tested |
| `japanese/jlpt` | reviewed-not-session-tested |

See `docs/COACH_PACK_CONTRACT.md` before adding or changing a coach.
